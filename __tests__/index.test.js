import { render, screen } from "@testing-library/react";
import Home from '../src/pages/index';

describe('Home', () => {
    test('renders a heading', () =>{
        //Arrange
        // render(<Home />);
        // //Act
        // const heading = screen.queryByText('welcome', { exact: false });
        // //Assert
        // expect(heading).not.toBeNull();
    })
})